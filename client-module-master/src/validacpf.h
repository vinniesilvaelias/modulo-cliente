#ifndef VALIDACPF_H
#define VALIDACPF_H

#include <iostream>

#include <algorithm>

using std::string;

void split(string& data);
int quoc(int sum);

int mult(char n1,int n2);

char getDigit(string data);

bool valid(string data);

#endif // VALIDACPF_H
